(defwidget status []
  (box
    :class "status"
    :valign "start"
    (label
      :visible { volume.out.mute || volume.out.percentage == 0 }
      :text { icons["volume-mute"][arraylength(icons["volume-mute"])-1] }
    )
    (label :text { icons[network.icon][round(network.percentage * (arraylength(icons[network.icon])-1) / 100, 0)] } )
    (label :text "󰁹" )
  )
)
